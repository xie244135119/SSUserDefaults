# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ios' do
  # use_frameworks!

	pod 'Objective-LevelDB'

end

target 'SSUserDefaults' do
  # use_frameworks!
	pod 'Objective-LevelDB'

  target 'SSUserDefaultsTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
